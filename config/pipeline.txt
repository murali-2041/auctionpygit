pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                 echo "this is first"
                 sh '''
                 python3 config/hello.py
                 '''
            }
        }
    }
}

